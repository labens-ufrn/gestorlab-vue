export interface Genero {
  id: string;
  title: string;
}

export interface Permission {
  id: string;
  title: string;
}

export interface Imagefile {
  name: string;
  type: string;
  size: number;
}